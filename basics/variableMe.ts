let greetings: string = "Hello chaieee";

// greetings = 6;   //throws an error that a number cannot be asigned to a variable of type string

let mynum = 6

// mynum.toUpperCase();    //Property 'toUpperCase' does not exist on type 'number'.

console.log(greetings);

//number
let userId: number = 334455
// userId.
      //when i use . after userId variable and just shows me functions of type number

//boolean
let isLoggedIn: boolean = false


export {}